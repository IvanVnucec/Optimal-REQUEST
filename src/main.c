#include "main.h"

#include <math.h>
#include <stdio.h>

int main(int argc, const char *const argv[])
{
    (void)argc;
    (void)argv;

    printf("Hello world!\n");
    printf("sqrt(2) = %f\n", sqrt(2.0));

    return 0;
}
